"Hey guys, pull the repo. To set up the entire backend locally so we can test the APIs tomorrow, just do this:
1. Double-click setup_env.bat (This installs Python/Conda).
2. Open 'Anaconda Prompt' from your Start menu.
3. Type conda activate hackathon.
4. Run setup.bat (This installs all the AI libraries).
Don't touch the backend code tonight, just get the environment ready!"


cmd for branch - 
git checkout -b <ur-branch>

cmd for pull and merge - 
git fetch
git checkout main
git pull
git checkout <ur-branch>
git merge main

cmd for push - 
git add .
git commit -m "<commit msg>"
git push -u origin <urb-branch>

then create the Pull Request on github website under this repo
